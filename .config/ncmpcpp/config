##### connection settings #####
#
mpd_host = 127.0.0.1
#
mpd_port = 2434
#
#mpd_connection_timeout = 5
#
## Needed for tag editor and file operations to work.
##
# mpd_music_dir = /run/media/othi/linux-data/Music

ncmpcpp_directory = ~/.config/ncmpcpp
lyrics_directory = ~/Music
# lyrics saved in music dir, see store_lyrics_in_song_dir
mpd_music_dir = ~/Music


colors_enabled = yes
empty_tag_color = 240
header_window_color = white
volume_color = 13
state_line_color = white
state_flags_color = 138
main_window_color = white
color1 = white
color2 = red
# main_window_highlight_color = red
progressbar_color = 234
progressbar_elapsed_color = white
statusbar_color = 240
alternative_ui_separator_color = white
# active_column_color = 138


song_list_format = {%a - }{%t}|{$8%f$9}$R{$3(%l)$9}

song_status_format = {{%a{ "%b"{ (%y)}} - }{%t}}|{%f}

song_library_format = {%n - }{%t}|{%f}

alternative_header_first_line_format = $b$1$aqqu$/a$9 {%t}|{%f} $1$atqq$/a$9$/b

alternative_header_second_line_format = {{$4$b%a$/b$9}{ - $7%b$9}{ ($4%y$9)}}|{%D}

current_item_prefix = $(yellow)$r

current_item_suffix = $/r$(end)

current_item_inactive_column_prefix = $(white)$r

current_item_inactive_column_suffix = $/r$(end)

now_playing_prefix = $b

now_playing_suffix = $/b

browser_playlist_prefix = "$2playlist$9 "

selected_item_prefix = $6

selected_item_suffix = $9

modified_item_prefix = $3> $9

##
## Note: attributes are not supported for the following variables.
##
song_window_title_format = {%a - }{%t}|{%f}
##
## Note: Below variables are used for sorting songs in browser.  The sort mode
## determines how songs are sorted, and can be used in combination with a sort
## format to specify a custom sorting format.  Available values for
## browser_sort_mode are "name", "mtime", "format" and "noop".
##

browser_sort_mode = name

browser_sort_format = {%A - }{%t}|{%f} {(%l)}

song_columns_list_format = (15)[]{A:Circle} (30)[cyan]{b} (4f)[white]{dr:Dsc}(4f)[white]{Nr:Tr} (50)[white]{t|f:Title} (5f)[white]{yr} (8f)[magenta]{lr:Time}
centered_cursor = no
progressbar_look = |

media_library_primary_tag = album_artist
default_find_mode = wrapped
allow_for_physical_item_deletion = no
search_engine_default_search_mode = 2
external_editor = vim
cyclic_scrolling = yes
user_interface = alternative
display_volume_level = no
jump_to_now_playing_song_at_start = yes
display_bitrate = yes
lines_scrolled = 2
search_engine_display_mode = columns
execute_on_song_change = "source ~/.config/ncmpcpp/notify.py"
# execute_on_player_state_change = "source ~/.config/ncmpcpp/notify.py"
autocenter_mode = yes
